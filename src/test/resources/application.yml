spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password:

  data:
    redis:
      host: localhost
      port: 6379

jwt:
    secret: test-secret-key-at-least-256-bits-long-for-hs256-algorithm-security-studylog-project-test
    access-token-expiration: 3600000
    refresh-token-expiration: 604800000
