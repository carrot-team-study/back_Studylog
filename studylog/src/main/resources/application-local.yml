server:
  port: 8081
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/studylog
    username: studylog
    password: studylog12349876
    driver-class-name: org.postgresql.Driver

  data:
    redis:
      host: localhost
      port: 6379
      password: ${REDIS_PASSWORD}

